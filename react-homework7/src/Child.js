export default function Child() {
    return(
        <div>
            Child Component
            <br/>
            name prop : {props.name}
            user prop : {props.user}
        </div>
    )

}